<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.JetNewsCopy" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/lavender</item>
        <item name="colorPrimaryVariant">@color/lavender</item>
        <item name="android:statusBarColor">@color/lavender</item>
    </style>

    <style name="Theme.JetNewsCopy" parent="Base.Theme.JetNewsCopy" />

    <style name="Theme.Navigation" parent="Base.Theme.JetNewsCopy">
        <item name="colorControlHighlight">@android:color/transparent</item>
    </style>
    
    <style name="App.Custom.Indicator" parent="Widget.Material3.NavigationRailView.ActiveIndicator">
        <item name="android:color">@color/lavender_medium</item>

    </style>

</resources>